import TruncateContainer from "./truncateContainer";

describe(TruncateContainer, () => {
  it("function truncatate should splice words after argmuent lenghMax", () => {
    expect(true).toEqual(true);
  });
});
